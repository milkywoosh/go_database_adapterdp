Pemisahan adapter tiap Payment HARUS dilakukan untuk memudahkan testing.



try to make adapter between PG and ORACLE



=================================================

Interface payment
type PaymenProccess interface {
    Pay(float64) string
}

misal:
type Paypal struct{} ===> harus bikin adapter tersendiri
func PaymentProccess() {}

type Stripe struct{} ===> harus bikin adapter tersendiri
func ChargeAmount() {}


tbl := "C##BOOK_STORE"
	// ORA_DB_PW_
	pws := "Qwerty123."
	// ORA_DB_CONN_STRING
	addr := "localhost:1521/orcl"
	// ORA_DB_LIB_DIR
	libdir := "C:\\oracle\\instantclient_21_6\\windows"


note clear test cache
==> go clean -testcache


note implement adapter pattern for using 2 different database

project
├── cmd                    # Command-related files
│   └── app                # Application entry point
│       └── main.go        # Main application logic
├── internal               # Internal codebase
│   ├── user               # Domain 'user'
│   │   ├── handler.go     # User-specific handler
│   │   ├── service.go     # User-specific service
│   │   ├── repository.go  # User-specific repository
│   │   └── user.go        # User model
│   └── purchase            # Domain 'product'
│       ├── handler.go     # Product-specific handler
│       ├── service.go     # Product-specific service
│       └── repository.go  # Product-specific repository
├── pkg                    # Shared utilities
├── configs                # Configuration files
├── go.mod                 # Go module definition
└── go.sum                 # Go module checksum file


============================================================
generate swagger-GIN

import
_ "github.com/swaggo/files"
_ "github.com/swaggo/swag"

go to cmd/app directory where main.go existed
run swag init

add _ "github.com/luke_design_pattern/cmd/app/docs" in main.go
make sure it show where docs dir is located
- add command @title @version and etc to auto generate to swagger
- add ==> 	router.GET("/swagger/*any", swagger.WrapHandler(swaggerFiles.Handler)) inside router management
- run "swag init"
- run app

swag init --generalInfo cmd/app/main.go --output cmd/app/docs --parseDependency --parseInternal
swag init --generalInfo ./cmd/app/main.go --output ./cmd/app/docs --parseDependency --parseInternal